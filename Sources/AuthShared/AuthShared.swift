/// Namespace enum for the AuthShared module.
///
/// `AuthShared` contains the request/response types, DTOs, and shared value types
/// that are consumed by both `AuthClient` (iOS/macOS) and `AuthServer` (Vapor).
/// No concrete logic lives here — the enum is intentionally uninhabited and serves
/// purely as a namespace anchor.
public enum AuthShared {
    // TODO: Add your implementation here.
}
