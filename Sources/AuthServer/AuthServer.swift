/// Namespace enum for the AuthServer module.
///
/// `AuthServer` groups the Vapor route controllers, Fluent models, migrations, JWT
/// payloads, and middleware that together form the server-side authentication layer.
/// No concrete logic lives here — the enum is intentionally uninhabited and serves
/// purely as a namespace anchor.
public enum AuthServer {
    // TODO: Add your implementation here.
}
